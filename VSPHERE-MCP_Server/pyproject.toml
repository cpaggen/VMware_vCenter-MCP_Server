[project]
name = "vsphere-mcp-server"
version = "0.1.0"
description = "A Model Context Protocol server for finding VMware VMs by MAC address"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=0.1.0",
    "python-dotenv>=1.0.0",
    "pyvmomi>=8.0.0",
]
